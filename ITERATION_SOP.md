# ITERATION_SOP.md
# Claw 疊代作業標準流程

版本：v1.4（新增 PR 疊代管理章節）
適用範圍：所有 OpenClaw 疊代任務
狀態：repo 根目錄正式版；OpenClaw workspace 應與此檔同步

---

## 總原則

1. **不得自行宣稱完成** — 所有「完成」判斷必須有實際指令輸出為依據。
2. **不得捏造輸出** — Test result 區塊必須貼真實 terminal 輸出，不得改寫、選擇性省略錯誤，或把失敗寫成成功。
3. **兩個 Phase 必須分開執行** — Phase 1 完成後，先列出 `## Phase 1 完成清單`，再進入 Phase 2；不可把實作、驗證、PR 一次混寫成完成。
4. **沒有 Validation checklist 就停下來** — 若設計文件缺少 `## Validation checklist` 區塊，Claw 必須停止並要求補上，不得自行決定驗證範圍。
5. **設計稿與 PR 文件一律用 `.md` 保存** — 設計稿、設計討論整理稿、PR 文件，都應整理成 `.md` 檔，方便後續討論與追溯。
6. **設計前必做現狀分析** — 凡設計方案、規劃 PR、制訂 SOP 等「創造性」任務，必須先打開關鍵檔案驗證現狀；禁止只依賴記憶或假設就開始設計。

---

## 設計前必做：現狀分析清單

在寫任何設計稿之前，必須先執行以下驗證：

```
□ 目錄結構檢查（dir app/, dir tests/, dir translation_tool/）
□ 關鍵檔案讀取（conftest.py, exceptions.py, theme.py, view_registry.py 等）
□ 工具可用性驗證（ruff, pytest, findstr 等）
□ 實際數據統計（fixture 數量、import 數量、檔案大小等）
□ 交叉比對記憶與現狀（記憶可能過時）
□ 交付前 self-check（設計稿完成後自己跑一次驗證）
```

**驗證失敗處理**：
- 若現狀與記憶不符：以現狀為準，標註「與記憶不符，已修正」
- 若無法驗證：明確說「無法驗證」，並列出嘗試過的工具

---

## 必須停下來問的情況
以下情況 Claw 不得自行把現況當契約，必須先問：
1. 不是單純 refactor，而是在定義什麼叫正確行為
2. 測試是在保舊行為，但無法確定那是不是 bug
3. caller 很多，但要刪的是 façade / shim / re-export
4. 某個舊行為看起來醜，但不確定是不是外部依賴
5. 設計稿沒有明講「這個舊行為要保還是要改」

---

## Phase 1：執行任務

### 1.1 讀取設計文件
- 找到本次任務對應的設計文件。
- 確認目標、範圍、以及明確列出的「不做的事」。
- 確認設計文件內已包含 `## Validation checklist`；若沒有，停下來要求補上。

### 1.2 執行
- 依設計文件逐項執行。
- 每完成一個子項目，內部記錄：已做 / 未做 / 無法做（原因）。
- 若涉及修改檔案，先建立可回退備份，再繼續下一輪修改。

### 1.3 Phase 1 完成檢查點
執行完畢後，列出以下清單後停下，**等待進入 Phase 2**：

```markdown
## Phase 1 完成清單
- [x] 做了：<項目>
- [x] 做了：<項目>
- [ ] 未做：<項目>（原因：）
- [ ] 超出範圍：<項目>
```

---

## Phase 2：驗證 + 寫 PR

### 2.1 執行 Validation Checklist
設計文件中必須包含 `## Validation checklist` 區塊。Claw 必須逐一執行，並擷取完整輸出。

格式範例：

```markdown
## Validation checklist
- [ ] `uv run pytest`
- [ ] `python -c "import app.views.cache_controller"`
- [ ] <其他驗證指令>
```

**規則：**
- 每條指令都必須實際跑，不得跳過。
- 輸出必須完整貼上，包含錯誤訊息。
- 若驗證失敗，必須記錄失敗原因，不得隱藏。
- 若某條指令無法執行，必須明確說明原因，不得偽裝成成功。

---

### 2.2 寫 PR / 設計文件
驗證完成後，依 template 產出文件。

**儲存路徑：** `docs/pr/YYYY-MM-DD_HHmm_PR_<topic>.md`

這條同時適用於：
- 正式 PR 文件
- 設計稿
- 設計討論整理稿

---

## PR Template

```markdown
# PR Title
<type>: <description>

# PR Description

## Summary
<本次 PR 的目的，1–3 句>

---

## Phase 1 完成清單
- [x] 做了：<項目>
- [ ] 未做：<項目>
- [ ] 超出範圍：<項目>

---

## 刪除/移除/替換說明（若有，固定放這裡）
### 刪除項目：`<名稱>`
- **為什麼改**：
- **為什麼能刪**：
- **目前誰在用 / 沒人在用**：
- **替代路徑是什麼**：
- **風險是什麼**：
- **我是怎麼驗證的**：

---

## What was done

### 1. <子項目標題>
<具體描述做了什麼，列出新增/修改的檔案>

### 2. <子項目標題>
...

---

## Important findings
<執行過程中發現的重要問題或資訊>

---

## Rejected approaches
- 試過：<做了什麼>
- 為什麼放棄：<哪個 test / 錯誤訊息 / 風險擋下來>
- 最終改採：<替代方案>（若有）

---

## Not included in this PR
這個 PR **沒有做** 以下事情：
- 沒有 <項目>
- 沒有 <項目>

---

## Next step

### PR N+1
- <建議下一步>

---

## Test result
```text
<貼上 Validation checklist 的實際指令輸出，完整，不得省略>
```
```

---

### 2.3 Rejected approaches 欄位標準（強制）
每份設計稿 / 設計討論稿 / 最終 PR 文件，都必須包含 `## Rejected approaches` 區塊，且固定放在 `## Not included in this PR` 前面。

固定格式：

```markdown
## Rejected approaches
- 試過：<做了什麼>
- 為什麼放棄：<哪個 test / 錯誤訊息 / 風險擋下來>
- 最終改採：<替代方案>（若有）
```

**強制要求：**
- 每份設計稿至少要有一條 Rejected approach。
- 若真的沒有嘗試過其他方案，不得留空；必須明確寫：
  - `試過：只考慮過一個方案`
  - `為什麼放棄：未嘗試其他方案，理由是：<為什麼此題只有一個合理方案>`
  - `最終改採：無`
- 這個欄位的目的，是強迫在 Phase 1 前先思考：
  - 有沒有更好的做法？
  - 有沒有看似可行但風險更高的方案？
  - 最後為什麼選現在這條路？
- 不得只寫空泛句子，例如「沒有其他方案」而不說理由。
- 若曾試過別的方案卻沒寫，視為文件不完整。

---

### 2.4 刪除/移除/替換說明標準（強制）
凡 PR 涉及以下任一動作：
- 刪除 function / class / method / field
- 刪除 import / module / package / file
- 移除 wrapper / façade / compatibility shim
- 停用既有流程並改走替代路徑
- 將某段舊邏輯替換為新實作

PR 文件中必須對**每一個刪除項目**逐項補上以下六個欄位，且**固定放在 `## Phase 1 完成清單` 後面**：

```markdown
### 刪除項目：`<名稱>`

- **為什麼改**：（這段為什麼存在、原本的意圖是什麼）
- **為什麼能刪**：（確認可刪的依據，例如：全域 grep 無引用、已有替代品）
- **目前誰在用 / 沒人在用**：（引用點清單，或確認無引用的方式）
- **替代路徑是什麼**：（如果有替代，替代在哪；如果沒有，說明不需要替代）
- **風險是什麼**：（刪錯或漏改的後果、有無 runtime 爆點）
- **我是怎麼驗證的**：（用什麼指令、輸出是什麼）
```

**強制要求：**
- 若任一欄位資訊不足，必須明確標記 `[需確認]`。
- 不得用「應該」「看起來」「大概沒人用」這類模糊語句帶過。
- 若聲稱「無引用」，必須附實際搜尋方式或指令。
- 若聲稱「已有替代」，必須寫出替代路徑與現行 caller。
- 若本 PR 沒有刪除項目，可省略這個章節；若有，不得改放其他位置。

---

## 違規情境與處理

| 情境 | 錯誤行為 | 正確行為 |
|------|----------|----------|
| 測試有錯誤 | 省略錯誤，寫「測試通過」 | 完整貼出錯誤，標記為既有問題或新問題 |
| 某項目沒做到 | 在 PR 中略過不提 | 列入「Not included in this PR」 |
| 驗證指令無法執行 | 寫「已驗證」 | 說明無法執行的原因，不得偽裝成成功 |
| 設計文件沒有 Validation checklist | 跳過驗證 | 停下來，通知 James 補上 checklist 後再繼續 |
| Rejected approaches 留空 / 缺漏 | 直接略過方案比較 | 至少寫一條，若只考慮一個方案也要明確寫理由 |
| 有刪除項目但未寫刪除理由 | 只寫「已移除」 | 逐項補上六欄位刪除說明 |

---

## 給 James 的提醒
每份設計文件都必須包含：

```markdown
## Validation checklist
- [ ] <指令 1>
- [ ] <指令 2>
```

沒有這個區塊，Claw 應停止並提示補上，不得自行決定驗證範圍。

---

## PR 疊代與分支管理

### 核心原則：單人開發，一次只有一個 open PR 在同一區塊

單人開發時，衝突的根因只有一個：**上一個相關 PR 還沒合併，就開始下一個並改到同一個檔案**。

只要遵守以下原則，衝突機率趨近於零。

---

### 規則 1：同一區塊，先合併先贏

目標檔案 / 功能區塊，在同一時間只能有一個 open PR。

```
✅ PR57 合併進 main → 再從最新 main 開 PR58
❌ PR57 還沒合併，就開始做 PR58 並改同一個函式
```

---

### 規則 2：開 branch 的正確時機

**正確時機**：目標 PR 已經合併進 main，確定 base 是最新的。  
**錯誤時機**：目標 PR 還在 open 狀態，你已經在本地改同一個檔案。

如果需要「先規劃設計」再開 branch：
1. 先在其他地方（Notion、Markdown 文件）做設計
2. 目標 PR 合併後，再從最新 main 建立 branch，套用設計實作

---

### 規則 3：同一區塊內可以拆分，但不建議疊加

**可以**：同一功能切成多個獨立的 PR，陸續合併（sequential stacking）

```
PR57a（底層）→ 合併進 main
PR57b（依賴 a）→ 合併進 main
PR57c（依賴 b）→ 合併進 main
```

**不建議**：同一功能做多層疊加（nested stacking）

```
feat/pr57-base → PR merge 後再合併
  └── feat/pr57-search-ui  → PR merge 前就疊上去
       └── feat/pr57-perf   → 最上層，等最久，風險最高
```

疊加風險：
- 中間層 amend 後，上游需要手動更新 base
- 容易忘記自己在哪一層，amend 錯 branch
- 衝突時修復複雜度 O(n²)

---

### 規則 4：需要同時處理多個功能區塊時

如果真的有兩個完全不相關的功能要同時做：

```
main
  ├── feat/pr57-icon-refactor    # icon_view 改版
  └── feat/pr58-settings-ui     # 設定頁改版（完全不同的檔案）
```

**前提**：兩個 PR 不能改到同一個檔案。若有交集，必須等其中一個合併後再做另一個。

---

### 規則 5：Branch 落後時的處理

如果因為特殊原因，branch 落後了 main 超過 1 個 PR：

**不要累積**，儘快 rebase：

```bash
git fetch origin
git rebase origin/main
# 解決衝突（通常是小範圍）
git push --force-with-lease
```

**何時用 rebase vs merge**：
- branch 落後不超過 3 個 commit → `git rebase origin/main`
- branch 落後很多，或有大量複雜衝突 → `git merge origin/main`（保留歷史，衝突一次解完）

---

### 觸發條件速查

| 情境 | 行動 |
|------|------|
| 想要開始新 PR，但上一個相關 PR 還沒合併 | 等合併後再開 branch |
| 兩個 PR 改到同一個檔案 | 等其中一個合併，再做另一個 |
| Branch 落後超過 1 個 PR | 儘快 rebase，不要累積 |
| 需要同時做多個相關功能 | 拆成 sequential（a→b→c），不要 nested stacking |
| 完全不相關的功能可以同時做 | 各自獨立的 branch，完全不重疊即可 |

---

### 簡短版（背誦用）

> **一個區塊，一次一個 PR。上一個合併，才做下一個。Branch 落後就 rebase，不要累積。**
